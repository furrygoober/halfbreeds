# site
# https://halfbreeds.internet-slackers.us

## debug commands:
(def state (:state (second (last (:lobbies @web.app-state/app-state)))))
(pprint (game.core/all-installed state :corp))
(pprint (game.core/all-installed state :runner))

state

## commands

npm run cljs:build
npm run css:build
lein fetch --local ../halfbreed-data
lein fetch --no-card-images --local ../halfbreed-data
lein create-indexes
lein repl

# docker related stuff
docker-compose up --build

docker exec -it halfbreeds-server-1 /bin/bash
    lein fetch -d --no-card-images -r furrygoober/halfbreed-data
    lein create-indexes


#### other ####
# check identity influence rules
./src/cljc/jinteki/validator.cljc
